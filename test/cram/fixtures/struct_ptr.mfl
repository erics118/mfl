extern int printf(char* fmt, ...);

struct Point {
    int x;
    int y;
};

int main() {
    struct Point p;
    p.x = 3;
    p.y = 4;
    struct Point *ptr;
    ptr = &p;
    printf("%d\n", ptr->x);
    printf("%d\n", ptr->y);
    ptr->x = 100;
    printf("%d\n", p.x);
    return 0;
}
