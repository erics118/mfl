extern int printf(char* fmt, ...);

struct Small {
    int x;
    int y;
};

struct Big {
    int a;
    long b;
};

int main() {
    printf("%d\n", sizeof(struct Small));
    printf("%d\n", sizeof(struct Big));
    return 0;
}
