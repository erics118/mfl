extern int printf(char* fmt, ...);

int main() {
    int a = 0;
    int b = 1;
    for (int i = 0; i < 10; ++i) {
        printf("%d\n", a);
        int next = a + b;
        a = b;
        b = next;
    }
    return 0;
}
